#definition 

For a composition of functions $f \circ g(x)=f(g(x))$ it is often cleaner to set a new variable $u=g(x)$.

So $f \circ g(x)=f(u)$, so the chain rule can be written:$$\frac{df}{dx}=\frac{df}{du} \frac{du}{dx}$$
