#definition 

If $f:X \to Y$ is a linear map: 
+ The *kernel*, or *null space* of $f$ is the set of vectors that map to $\mathbf{0}$, ie $$\{ \mathbf{x} \in X.f(\mathbf{x})=\mathbf{0} \}$$
+ The kernel forms a vector space: **all** linear combinations of kernel elements are in the kernel.
