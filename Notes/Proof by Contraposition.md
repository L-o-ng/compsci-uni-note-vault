#definition 

To prove $P \implies Q$, instead prove $\text{not }Q \implies\text{not }P$
For example, 
+ "if $n^2$ is even, then $n$ is even" 
is logically equivalent to,
+ "if $n$ is odd then $n^2$ is odd".