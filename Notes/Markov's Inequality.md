#definition 

Let $X$ be a non-negative random variable:
$$
P(X\geq a)\leq \frac{E(X)}{a}\forall a>0
$$

This can give a loose bound, but makes no assumptions at all about the form of the distribution.