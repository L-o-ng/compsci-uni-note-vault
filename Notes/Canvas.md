#notes 

Instead of loading a static image, we can use the browser drawing API to paint into memory, and upload that as a texture.
We can use this for:
+ [[Billboards]];
+ Scoreboards/dashboards;
+ Procedural noise/gradients;
+ etc.

![[Canvas.png]]