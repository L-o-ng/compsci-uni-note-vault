#notes 

An authorisation graph contains *all* the relationships between subjects and objects. For example: a social graph on a social network, or healthcare systems.
A policy can simply check if a particular path exists in the graph.
![[RS-B AC.png]]