#definition 

Let $\mathscr{M}(x,y)$ be a TM that expects two-part input $x \sqcup y$. There exists a TM $\mathscr{IMN}(t,x)$ that on inputs $\langle M \rangle$ and $x$, produces a description of a TM $\langle M_{x} \rangle$ such that for every $y$, $\mathscr{M}_{x}(y)=\mathscr{M}(x,y)$.
